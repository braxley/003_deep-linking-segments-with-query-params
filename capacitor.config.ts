import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '001a_segment-with-dynamic-component_ionic',
  webDir: 'dist'
};

export default config;
